/**
 * The default tag value.
 */
export const DEFAULT_TAG_VALUE = Symbol('default_tag_value');
/**
 * The key of the instances in the transaction.
 */
export const TRANSACTION_INSTANCES_KEY = Symbol('transaction_instances_key');
/**
 * The key of the default instance in the transaction.
 */
export const TRANSACTION_DEFAULT_INSTANCE_KEY = Symbol('transaction_default_instance_key');
